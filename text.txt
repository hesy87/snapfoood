https://snappfood.ir/restaurant/menu/%D9%BE%DB%8C%D8%B2%D8%A7%D8%B1%D9%88__%D9%88%D9%84%DB%8C%D8%B9%D8%B5%D8%B1_-r-0mvvzy

////////////////lifting state up

parent 
<ShoppingButton onSelectOrderCount={selectOrderCount}/>

const selectOrderCount = (orderCount) => {
    setOrderCount(orderCount)
    console.log(orderCount);
}

child 

props.onSelectOrderCount(orderCount);

//////////////context 

1-file1 context
import React from "react";

const CartContext = React.createContext({
  item: [],
  totalAmount: 0,
  addItem: () => {},
  removeItem: (id) => {},
});

export default CartContext;

2-file2 provider
import CartContext from "./selectedFood-context";

const Cartprovider = (props) => {
    const addItemToCartHandler = item => {};
    const removeItemFromCartHandler = id => {};
    const cartContext = {
        item: [],
        totalAmount: 0,
        addItem: addItemToCartHandler,
        removeItem: removeItemFromCartHandler,
    };

  return <CartContext.Provider value={cartContext}>{props.children}</CartContext.Provider>;
};

export default Cartprovider;

3-appjs
<Cartprovider>.....</Cartprovider>
